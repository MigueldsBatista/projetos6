[project]
name = "projeto-trt6"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "shared",
    "apps/ingestion",
    "apps/worker-llm",
    "apps/worker-scrapper",
    "apps/api",
]
